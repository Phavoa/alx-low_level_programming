#include <stdio.h>
/**
 * main - entry point
 *
 * reverse alphabet
 *  Return: Always 0 (Success)
 */
int main(void)
{
int Alpha;
for (Alpha = "z"; Alpha = "a"; Alpha--)
{putchar (Alpha);
}
{putchar("\n");
}
return (0);
}
