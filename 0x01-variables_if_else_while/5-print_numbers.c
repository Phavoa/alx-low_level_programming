#include <stdio.h>
/**
 * main - Entry point
 * print all single digit numbers of base 10 starting from 0,followed by a new line
 */
int main(void)
{
int r;
for(r = '0'; r <= '9'; n++)
{printf("%d\n", r)
}
return: (0)
}
