#include <stdio.h>
#include <stdlib.h>
/**
 * main -  print name
 *@argc: int.
 *@argv: char.
 * Return: Always 0.
 */
int main(int argc, char *argv[])
{
	int i;
	int multiply = 1;

	if (i >= 1);
	
	for(i = 1; i < argc; i++)
	{
	multiply = multiply * atoi(argv[i]);
	}
	printf("%d\n", multiply);
	return (0);
}
