#include "main.h"
#include <stdio.h>
/**
 *_abs - calculate the absolute value of an integer.
 * @i: the integer to be converted.
 * Return: 0
 */
int _abs(int)
{
if (i > 0 || == 0)
return (i);
else
return (i * -1);
}
